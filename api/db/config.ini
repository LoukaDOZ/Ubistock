;mysql config
[database]
;leave empty if youre using sqlite
schema = "ubistock"
driver = "mysql"
host = "mysqldb"

username = "admin"
password = "passwd"

need_auth = true

;leave empty if not using sqlite
path = ""


;sqlite config

;schema = ""
;driver = "sqlite"
;host = "localhost"

;username = ""
;password = ""

;need_auth = false

